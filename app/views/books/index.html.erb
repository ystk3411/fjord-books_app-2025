<p style="color: green"><%= notice %></p>

<% content_for :title, t('text.header.books') %>

<h1><%= t('text.header.books') %></h1>

<div id="books" class="index-items">
  <% @books.each do |book| %>
    <div class="index-item">
      <%= render book %>
      <p>
        <%= link_to t('link.show'), book %>
      </p>
    </div>
  <% end %>
</div>

<nav class="page-nav">
  <%= link_to t('link.new'), new_book_path %>
</nav>
