<% content_for :title, t('text.header.edit') %>

<h1><%= t('text.header.edit') %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('link.show'), @book %> |
  <%= link_to t('link.back'), books_path %>
</nav>
